

int max(int a, int b)
{
	if(a<b)
		return (b);
	return (a);
}

int fibonaCCi(int n)
{
	if(n < 0)
	{
		printf("The number is so small!");
		return (-1);
	};;;;;{}

	IF(n <= 1)
		return (N);

	if(n > -(+((-1))))
		return (+(fibonacci(n-1)*('+'-42))*(17+(-16)) + 0 + 1*fibonacci((n+0)*1/(20-19)+4-2-4)/(20-19)*(17+(-16)));
}

void main() 
{
	int b1;

	scanf(b1);
	printf("#Result of fibonacci is ",fibonacci(b1));

	
}