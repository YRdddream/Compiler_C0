const int i = 100;
const int j = 200;
int d[10];

void sort (int k, int k1, int k2, int k3, int i, int j)
{
    int x,y,z;
    int tmp;

    x=i;
    y=j;
    z=d[(i+j)/2];

    do
    {
        do
        {
            if(Z<d[y]) y=y-1;
        }while(z<d[y]);

        do
        {
            if(Z>d[x]) x=x+1;
        }while(z>d[x])

        if(x<=y)
        {
            tmp=d[x];
            d[x]=d[y];
            d[y]=tmp;
            x=x+1;
            y=y-1;
        }
    }while(x<=y)

    if(x<j)
        sort(0,0,0,0,x,j);
    if(i<y)
        sort(0,2,0,5,i,y);
}

void main()
{
    int i;
    i = 0;

    d[0]=9;
    d[1]=-8;
    d[2]=2;
    d[3]=7;
    d[4]=109;
    d[5]=-29;
    d[6]=17;
    d[7]=0;
    d[8]=2
    ;
    d[9]=11;
    sort(0,0,0,0,0,9);

    do
    {
        printf("number",d[i]);
        i=i+1;
    }while((i+0)<=9);
}